import React, { FC } from "react";

import NotFoundPageLayout from "../components/NotFoundPageLayout";

const NotFoundPageContainer: FC = () => {
  return <NotFoundPageLayout />;
};

export default NotFoundPageContainer;
